/**
 * Platform abstraction layer for NodeJS.
 */
export declare class PAL {
    /**
     * Perform any initialization for the PAL.
     * @returns Promise.
     */
    static initialize(): Promise<void>;
}
